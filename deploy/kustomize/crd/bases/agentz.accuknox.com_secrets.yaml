apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: secrets.agentz.accuknox.com
spec:
  group: agentz.accuknox.com
  names:
    kind: Secret
    listKind: SecretList
    plural: secrets
    singular: secret
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Owning Agent
      jsonPath: .spec.agentRef.name
      name: Agent
      type: string
    - description: Projected secret name
      jsonPath: .spec.key
      name: Key
      type: string
    - description: Secret type
      jsonPath: .spec.type
      name: Type
      type: string
    - description: Secret state
      jsonPath: .status.state
      name: Status
      type: string
    - description: Age of the Secret
      jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Secret is the Schema for the secrets API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Spec defines the desired Secret state.
            properties:
              agentRef:
                description: AgentRef identifies the owning Agent.
                properties:
                  name:
                    description: Name is the Agent name.
                    type: string
                required:
                - name
                type: object
              createdByUserID:
                description: CreatedByUserID is the immutable Better Auth User ID that created the resource.
                maxLength: 128
                minLength: 1
                type: string
              hosts:
                description: Hosts defines the allowed request hosts for secret injection.
                items:
                  type: string
                type: array
              key:
                description: Key is the logical secret name and projected environment variable name.
                type: string
              lastModifiedByUserID:
                description: LastModifiedByUserID is the Better Auth User ID that last changed the resource.
                maxLength: 128
                minLength: 1
                type: string
              oauth:
                description: OAuth configures one OAuth secret.
                properties:
                  authorizationEndpoint:
                    description: AuthorizationEndpoint is the OAuth authorization endpoint.
                    type: string
                  issuer:
                    description: Issuer identifies the OAuth issuer.
                    type: string
                  provider:
                    description: Provider identifies one catalog provider when known.
                    type: string
                  registrationEndpoint:
                    description: RegistrationEndpoint is the dynamic client registration endpoint.
                    type: string
                  resource:
                    description: Resource is the OAuth resource or audience.
                    type: string
                  scopes:
                    description: Scopes lists the requested OAuth scopes in order.
                    items:
                      type: string
                    type: array
                  tokenEndpoint:
                    description: TokenEndpoint is the OAuth token endpoint.
                    type: string
                type: object
              type:
                description: Type identifies the Secret kind.
                enum:
                - Static
                - OAuth
                type: string
            required:
            - agentRef
            - createdByUserID
            - hosts
            - key
            - lastModifiedByUserID
            - type
            type: object
            x-kubernetes-validations:
            - message: createdByUserID is immutable
              rule: self.createdByUserID == oldSelf.createdByUserID
          status:
            description: Status defines the observed Secret state.
            properties:
              conditions:
                description: Conditions represent the current Secret conditions.
                items:
                  description: Condition contains details for one aspect of the current state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastRefreshFailureMessage:
                description: LastRefreshFailureMessage is the most recent failed OAuth refresh message.
                type: string
              lastRefreshFailureReason:
                description: LastRefreshFailureReason is the most recent failed OAuth refresh reason.
                type: string
              lastRefreshFailureTime:
                description: LastRefreshFailureTime is the most recent failed OAuth refresh time.
                format: date-time
                type: string
              lastRefreshTime:
                description: LastRefreshTime is the most recent successful OAuth refresh time.
                format: date-time
                type: string
              lastRuntimeUpdateTime:
                description: LastRuntimeUpdateTime is the most recent time the runtime record changed.
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration is the latest reconciled generation.
                format: int64
                type: integer
              runtimeRef:
                description: RuntimeRef identifies the OpenBao runtime record.
                properties:
                  path:
                    description: Path is the OpenBao secret data path.
                    type: string
                required:
                - path
                type: object
              state:
                description: State is the high-level runtime state.
                enum:
                - Accepted
                - Ready
                - Degraded
                type: string
              tokenExpiryTime:
                description: TokenExpiryTime is the current OAuth access token expiry time.
                format: date-time
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
