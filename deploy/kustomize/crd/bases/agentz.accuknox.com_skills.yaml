apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: skills.agentz.accuknox.com
spec:
  group: agentz.accuknox.com
  names:
    kind: Skill
    listKind: SkillList
    plural: skills
    singular: skill
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Skill is the Schema for the skills API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of Skill.
            properties:
              description:
                description: Description summarizes when the skill should be used.
                maxLength: 1024
                minLength: 1
                type: string
              storagePath:
                description: StoragePath is the full object storage directory for the active version.
                minLength: 1
                type: string
              version:
                description: Version is the active stored version for all Agent and Sandbox references.
                format: int64
                minimum: 1
                type: integer
            required:
            - description
            - storagePath
            - version
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
