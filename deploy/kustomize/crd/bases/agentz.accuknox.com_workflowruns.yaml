---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: workflowruns.agentz.accuknox.com
spec:
  group: agentz.accuknox.com
  names:
    kind: WorkflowRun
    listKind: WorkflowRunList
    plural: workflowruns
    shortNames:
    - wfr
    singular: workflowrun
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Target agent
      jsonPath: .spec.agentName
      name: Agent
      type: string
    - description: Workflow name
      jsonPath: .spec.workflowName
      name: Workflow
      type: string
    - description: Execution phase
      jsonPath: .status.phase
      name: Phase
      type: string
    - description: Age of the WorkflowRun
      jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: WorkflowRun is the Schema for the workflowruns API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of WorkflowRun.
            properties:
              agentName:
                description: AgentName identifies the target AgentZ Agent.
                maxLength: 32
                minLength: 1
                pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                type: string
              inputs:
                description: Inputs contains execution inputs copied into this run.
                x-kubernetes-preserve-unknown-fields: true
              scheduleRef:
                description: ScheduleRef points back to the owning WorkflowSchedule
                  when created by cron.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              timeoutSeconds:
                description: TimeoutSeconds bounds total execution time for one run.
                format: int32
                maximum: 604800
                minimum: 1
                type: integer
              workflowName:
                description: WorkflowName identifies the saved workflow definition
                  for the Agent.
                maxLength: 32
                minLength: 1
                pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                type: string
            required:
            - agentName
            - workflowName
            type: object
          status:
            description: status defines the observed state of WorkflowRun.
            properties:
              completedAt:
                description: CompletedAt is when the run reached a terminal state.
                format: date-time
                type: string
              conditions:
                description: Conditions represent the current state of the WorkflowRun
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              message:
                description: Message contains the current error or completion summary.
                type: string
              nodes:
                description: Nodes tracks execution status for each node in the workflow
                  graph.
                items:
                  description: WorkflowRunNodeStatus defines the observed state of
                    one WorkflowRun node.
                  properties:
                    completedAt:
                      description: CompletedAt is when the node reached a terminal
                        state.
                      format: date-time
                      type: string
                    message:
                      description: Message contains current node context or failure
                        details.
                      type: string
                    name:
                      description: Name identifies the workflow node.
                      maxLength: 64
                      minLength: 1
                      pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                      type: string
                    phase:
                      description: Phase is the lifecycle state tracked by the executing
                        agent.
                      enum:
                      - Disabled
                      - Running
                      - Unacked
                      - Succeeded
                      - Failed
                      type: string
                    startedAt:
                      description: StartedAt is when the agent started executing the
                        node.
                      format: date-time
                      type: string
                  required:
                  - name
                  - phase
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              observedGeneration:
                description: ObservedGeneration is the latest reconciled generation.
                format: int64
                type: integer
              phase:
                description: Phase is the lifecycle state tracked by controllers and
                  the agent.
                enum:
                - Pending
                - Running
                - Unacked
                - Succeeded
                - Failed
                type: string
              sessionID:
                description: SessionID is the OpenCode session created for this run.
                type: string
              startedAt:
                description: StartedAt is when the controller started execution.
                format: date-time
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
