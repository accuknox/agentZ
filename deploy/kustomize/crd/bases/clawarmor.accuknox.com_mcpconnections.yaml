apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: mcpconnections.clawarmor.accuknox.com
spec:
  group: clawarmor.accuknox.com
  names:
    kind: MCPConnection
    listKind: MCPConnectionList
    plural: mcpconnections
    singular: mcpconnection
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Authentication mode
      jsonPath: .status.authMode
      name: Auth Mode
      type: string
    - description: Connection state
      jsonPath: .status.state
      name: Status
      type: string
    - description: MCP endpoint URL
      jsonPath: .spec.endpoint.url
      name: Endpoint
      type: string
    - description: Age of the MCPConnection
      jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: MCPConnection is the Schema for the mcpconnections API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Spec defines the desired state of MCPConnection.
            properties:
              auth:
                description: Auth defines optional upstream authentication settings.
                properties:
                  bearer:
                    description: Bearer configures static bearer-style credential injection.
                    properties:
                      location:
                        description: Location defines where the credential is injected upstream.
                        properties:
                          cookie:
                            description: Cookie injects auth into a cookie.
                            properties:
                              name:
                                description: Name is the cookie name.
                                type: string
                            required:
                            - name
                            type: object
                          header:
                            description: Header injects auth into an HTTP header.
                            properties:
                              name:
                                description: Name is the HTTP header name.
                                type: string
                              prefix:
                                description: Prefix is prepended to the resolved credential when set.
                                type: string
                            required:
                            - name
                            type: object
                          queryParameter:
                            description: QueryParameter injects auth into a query parameter.
                            properties:
                              name:
                                description: Name is the query parameter name.
                                type: string
                            required:
                            - name
                            type: object
                        type: object
                      secretRef:
                        description: SecretRef identifies the OpenBao record containing the credential.
                        properties:
                          key:
                            description: Key is the field within the OpenBao record.
                            type: string
                          path:
                            description: Path is the OpenBao secret data path.
                            type: string
                        required:
                        - key
                        - path
                        type: object
                    type: object
                  oauth:
                    description: OAuth configures OAuth-backed credential injection.
                    properties:
                      authorizationEndpoint:
                        description: AuthorizationEndpoint is the OAuth authorization endpoint.
                        type: string
                      issuer:
                        description: Issuer identifies the OAuth issuer.
                        type: string
                      location:
                        description: Location defines where the OAuth access token is injected upstream.
                        properties:
                          cookie:
                            description: Cookie injects auth into a cookie.
                            properties:
                              name:
                                description: Name is the cookie name.
                                type: string
                            required:
                            - name
                            type: object
                          header:
                            description: Header injects auth into an HTTP header.
                            properties:
                              name:
                                description: Name is the HTTP header name.
                                type: string
                              prefix:
                                description: Prefix is prepended to the resolved credential when set.
                                type: string
                            required:
                            - name
                            type: object
                          queryParameter:
                            description: QueryParameter injects auth into a query parameter.
                            properties:
                              name:
                                description: Name is the query parameter name.
                                type: string
                            required:
                            - name
                            type: object
                        type: object
                      registrationEndpoint:
                        description: RegistrationEndpoint is the dynamic client registration endpoint.
                        type: string
                      resource:
                        description: Resource is the OAuth resource or audience for the MCP endpoint.
                        type: string
                      scopes:
                        description: Scopes lists requested OAuth scopes in order.
                        items:
                          type: string
                        type: array
                      secretRef:
                        description: |-
                          SecretRef identifies the OpenBao record containing OAuth connection
                          material.
                        properties:
                          key:
                            description: Key is the field within the OpenBao record.
                            type: string
                          path:
                            description: Path is the OpenBao secret data path.
                            type: string
                        required:
                        - key
                        - path
                        type: object
                      tokenEndpoint:
                        description: TokenEndpoint is the OAuth token endpoint.
                        type: string
                    type: object
                type: object
              endpoint:
                description: Endpoint defines the remote MCP server endpoint and transport settings.
                properties:
                  headers:
                    additionalProperties:
                      type: string
                    description: Headers defines non-secret static headers sent to the upstream endpoint.
                    type: object
                  insecureSkipVerify:
                    description: InsecureSkipVerify disables upstream TLS certificate verification.
                    type: boolean
                  timeout:
                    description: Timeout bounds one upstream request to the MCP endpoint.
                    type: string
                  url:
                    description: URL is the canonical HTTPS URL of the upstream MCP endpoint.
                    type: string
                required:
                - url
                type: object
            required:
            - endpoint
            type: object
          status:
            description: Status defines the observed state of MCPConnection.
            properties:
              authMode:
                description: AuthMode is the resolved authentication mode ("None", "Bearer", or "OAuth").
                type: string
              authPolicyRef:
                description: AuthPolicyRef identifies the managed auth policy for the connection.
                properties:
                  name:
                    description: Name is the resource name.
                    type: string
                  namespace:
                    description: Namespace is the namespace containing the managed resource.
                    type: string
                required:
                - name
                - namespace
                type: object
              conditions:
                description: Conditions represent the current state of the MCPConnection resource.
                items:
                  description: Condition contains details for one aspect of the current state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              extAuthDeploymentRef:
                description: |-
                  ExtAuthDeploymentRef identifies the ext-auth Deployment for the
                  namespace.
                properties:
                  name:
                    description: Name is the resource name.
                    type: string
                  namespace:
                    description: Namespace is the namespace containing the managed resource.
                    type: string
                required:
                - name
                - namespace
                type: object
              extAuthServiceRef:
                description: |-
                  ExtAuthServiceRef identifies the ext-auth Service for the
                  namespace.
                properties:
                  name:
                    description: Name is the resource name.
                    type: string
                  namespace:
                    description: Namespace is the namespace containing the managed resource.
                    type: string
                required:
                - name
                - namespace
                type: object
              lastProbeTime:
                description: LastProbeTime is the most recent time an observer completed an MCP probe.
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration is the latest reconciled generation.
                format: int64
                type: integer
              serviceRef:
                description: |-
                  ServiceRef identifies the managed Service that fronts the upstream
                  connection runtime.
                properties:
                  name:
                    description: Name is the resource name.
                    type: string
                  namespace:
                    description: Namespace is the namespace containing the managed resource.
                    type: string
                required:
                - name
                - namespace
                type: object
              state:
                description: State is the high-level runtime state of the connection.
                enum:
                - Accepted
                - Ready
                - Degraded
                type: string
              toolCatalogReady:
                description: |-
                  ToolCatalogReady reports whether the most recent probe discovered the
                  upstream tool catalog successfully.
                type: boolean
              tools:
                description: Tools lists the most recently discovered upstream MCP tools.
                items:
                  description: MCPConnectionTool describes one discovered upstream MCP tool.
                  properties:
                    name:
                      description: Name is the upstream MCP tool name.
                      type: string
                  required:
                  - name
                  type: object
                type: array
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
